ANTHROPIC_API_KEY=your_key_here

# Optional: IP-based rate limiting for /api/recommend.
# Create a free Upstash Redis database at https://upstash.com and paste the
# REST URL and token below. If these are omitted, rate limiting is disabled.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
